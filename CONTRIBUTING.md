# Contributing

Keep each project self-contained in its existing directory. Add only the dependencies needed by that project, and document non-standard setup in that directory's README.

Do not commit local virtual environments, Python bytecode, SQLite databases, credentials, or generated output.
